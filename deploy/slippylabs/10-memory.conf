[Service]
# Soft limit: the kernel reclaims hard against this service instead of letting a
# leak page out the whole box. It throttles, it does not kill -- a hard cap would
# drop players mid-game. Set ~25x observed usage, so only a real leak trips it.
MemoryHigh=1G
MemoryAccounting=true
